1.把光标移动到某行上面按"J",就把这一行的换行符删掉进而和下一行合并了。
2.如果u两次发现只是恢复两次最后一步，那么就是配置成了vi兼容模式了，在|not-compatible|里面找到解决方法。
3.“U”撤销一行所有的操作，再“U”则对上次的“U”进行撤销（u和ctrl r也行了）。
4.ctrl+o跳回前一个位置（不同文件也可，非插入模式），<TAB>和它相反
5,关于:help的一些约定和操作：
p31
“||”之间的内容是链接，可以通过<ctrl> ]来跳入，例如：|help|。当然可以用ctrl o来后退。
