logsave [ -asv ] logfile cmd_prog [ ... ]
[功能]
把一个命令的输出，输出到一个指定的日志文件当中。

[描述]
logsave程序执行cmd_prog(可以具有参数)，然后把这个程序的输出拷贝到一个指定的文件logfile中，如果文件不存在那么就写在内存中直到可以写为止。写入文件的同时也会把输出输出到标准输出上面。
如果cmd_prog是"-"那么logsave将会把标准输入的内容保存到一个log文件中。
常用选项：
-a	把内容追加到logfile中，而不是替换。
-s 这个选项导致logsave忽略向log文件写入这样的字符串：字符串被control-A(ASCII为001或者头的开始)和control-B(ASCII为002或者文本的开始)括起来。这样，像进度条的信息就会可以在用户终端中被看见，但是却没有写入到log文件中。
-v 可以显示更多的信息。

[举例]
*把ls命令输出到文件中：
$logsave testOut ls

[其他]
这个命令内容很少，具体参见man。
