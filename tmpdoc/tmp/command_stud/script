script
好像是一个记录终端所有输入输出显示的工具，将所有的信息保存到一个文件中。

*记录终端的输出：
$script
这个命令会自动在当前目录下面生成一个typescript文件，然后以后所有的终端显示都会到这个文件中。如果想要停止，那么就运行$exit或者[Ctrl]d.这样就不再写入了。但是文件中有许多特殊字符。

*记录一个命令的输入输出：
$script -c ls
这样会将ls的输出和敲入的ls一并写入到typescript文件中。注意这个命令对于操作屏幕的程序不太好用。
