总体来看，操作和vim差不多。

*查看帮助信息：
输入":help".

*弹出插件框以便启用或者禁用vimperator:
输入":addons".

*调出菜单栏：
输入":set go+=mT".

*调出菜单栏和导航栏：
输入":set go+=mTB".
实际上，这里m是菜单栏，T是导航栏(可以输入网址)，B是书签栏。

*隐藏菜单栏：
输入":set go=".

*打开一个网址：
输入":o <你要进入的网址>".
输入网址的时候，可以按[Tab]匹配历史。

*新建立一个标签并打开网页：
输入":t <你要进入的网址>".

**想要跳到某个链接：
在原来的标签上打开新网址：
1)输入"f".
这时候会把所有链接标记上一个数字。如果这时想要取消跳转那么就输入[Esc].
2)输入那个链接对应的数字。
这时，如果是进入到一个输入框中，那么vimperator会进入插入模式，可以通过输入[Esc]退出插入模式。

在新标签上打开新网址：
1)输入"F".
这时候会把所有链接标记上一个数字。如果这时想要取消跳转那么就输入[Esc].
2)输入那个链接对应的数字。
这样会在一个新的标签页打开那个网址,当然输入框不会新开标签了。

这里，f/F后面可以输入字母，实现部分匹配。
**
*前进：
输入"[Ctrl]i".

*后退：
输入"[Ctrl]o".

*恢复关闭的标签：
输入"u".

*关闭当前标签：
输入"d".
或输入":q".

*新开一个空白的标签：
输入":tabnew".

*复制当前网址：
输入"yy".
