这里是一些shell脚本的例子。
部分内容是来自advanced bash shell这本书，经过稍微改造。

每个目录中的内容各自包含类似的功能的脚本。
./00_miscellaneous 目录中的内容杂乱还未好好分类，其分类有待改善。


=========
一些建议的shell编程风格：
1)判断变量的时候：
if [ x$VAR == xyes ]
then
...
fi
这里，判断变量VAR是否为yes，为了防止VAR变量为空，所以加上一个前缀'x'，所以变成了判断x+VAR是否为x+yes。
